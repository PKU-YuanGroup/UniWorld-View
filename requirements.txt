# Python 3.11 is required (matches bundled vipe_ext for CPython 3.11)

# Use the CUDA 12.1 wheel index for PyTorch pinned below
--extra-index-url https://download.pytorch.org/whl/cu121

# ===== Core GPU stack =====
torch==2.4.0+cu121
torchvision==0.19.0+cu121
torchaudio==2.4.0+cu121
triton==3.0.0
ninja==1.11.1.4
setuptools>=68
wheel

# ===== Model and diffusion libs =====
diffusers==0.35.0
transformers==4.48.3
accelerate==1.10.1
huggingface-hub==0.34.4
tokenizers==0.21.0
timm==1.0.14

# ===== Config, utils, and UI =====
hydra-core==1.3.2
omegaconf==2.3.0
gradio==3.45.0
einops==0.8.1
numpy==1.26.4
scipy==1.15.1
Pillow==10.4.0
opencv-python==4.11.0.86
imageio==2.37.0
imageio-ffmpeg==0.6.0
trimesh==4.6.2
carvekit==4.1.2

# ===== VIPE runtime deps =====
OpenEXR==3.2.4
kornia==0.8.0
viser==1.0.1
click==8.1.8
av==12.0.0
rerun-sdk==0.22.0
gdown==5.2.0
python-pycg==0.10.1

# ===== Optional: install local VIPE (builds CUDA extension) =====
# If the bundled extern/vipe binary (vipe_ext*.so) does not match your Python,
# build the extension against the current env with:
#   pip install -e extern/vipe
# (Requires a CUDA-enabled torch above and a working CUDA toolchain/NVCC.)
